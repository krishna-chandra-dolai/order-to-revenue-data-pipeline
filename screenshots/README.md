# Screenshots

Add only real, sanitized screenshots that materially support the project.
Before committing an image, check that it does not expose passwords, connection
strings, access keys, SAS tokens, subscription identifiers, user identities, or
unnecessary portal details. Machine-readable proof is kept in `docs/evidence/`.

No screenshots are required for the current evidence set; placeholder folders
for unimplemented services were removed.
