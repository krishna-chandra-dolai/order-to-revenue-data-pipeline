# Copy this file to .env only if you use a local environment-file loader.
# .env is ignored by Git. Never commit a real password.
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGDATABASE=order_revenue_db

# Deliberately omitted: PGPASSWORD
# Prefer PostgreSQL's interactive prompt or a user-level pgpass file.

